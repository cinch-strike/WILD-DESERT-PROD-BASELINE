<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cost_Price_GST_exc__c</fullName>
    <description>Cost Price formula will be based on the value of Cost Price (without quote) OR Quote_Item__r.Cost_Price_GST_exc__c</description>
    <formula>IF(NOT(ISBLANK( Item__c ))
  , Item__r.Cost_Price__c
  , IF(Quoted_Cost_Price__c &gt; 0, 
    Quoted_Cost_Price__c ,
    IF(NOT( ISBLANK( Quote_Item__c ) ), 
      Quote_Item__r.Cost_Price_GST_exc__c, 
      Cost_Price_without_quote__c 
    )
  )
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Cost Price (GST exc)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Currency</type>
</CustomField>
