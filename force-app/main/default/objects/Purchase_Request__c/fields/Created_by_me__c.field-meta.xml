<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Created_by_me__c</fullName>
    <formula>IF(NOT(ISBLANK(Destination_Store__c)),
  IF( (Destination_Store__r.Manager__c = $User.Id) || ( Destination_Store__r.Unit_Manager__c = $User.Id) || (CreatedById = $User.Id) , TRUE, FALSE),
  IF(CreatedById = $User.Id , TRUE, FALSE)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Created by me</label>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
